package frc.Auto.SwitchAutos;
/*
import frc.subSystems.Drive;
import frc.subSystems.Gamepad;
import frc.subSystems.Teleop;
*/
public class bSimpleSwitchAuto {
/*
    Drive mDrive;
    Gamepad mGamepad;
    Teleop mTeleop;
    int bSimpleCounter = 0;
    public boolean inTeleop3 = false;
*/
/*
    static bSimpleSwitchAuto mInstance= new bSimpleSwitchAuto();

    public static bSimpleSwitchAuto getInstance(){
        return mInstance;
    }

    public bSimpleSwitchAuto(){
        mDrive = Drive.getInstance();
        mGamepad = Gamepad.getInstance();
        mTeleop = Teleop.getInstance();
    }

    public void bSimpleSwitchAutoExecute(){
        if(mGamepad.getButtonA()){
            inTeleop3 = true;
        }
    
    
        if(inTeleop3==false){
            switch(bSimpleCounter){
                case 0:
                mDrive.ROTS(2,370);

                    if(mDrive.bRots){
                        mDrive.robotDrive.arcadeDrive(0, 0);
                        mDrive.bRots = false;
                        bSimpleCounter++;
                    }

                break;

                case 1:
                //Place
                break;

            }
        }
    }
*/
}